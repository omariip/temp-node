// CommonJS, every file is module (by default)
// Modules - Encapsulated code (only share minimum)


//not working anymore
const names = require('./4-names');
const sayHi = require('./5-hiFunc');
const data = require('./6-alternative');
require('./7-mind-grenade');

// console.log(data);
// sayHi('omar');
// sayHi(names.john);
// sayHi(names.peter);